<div class="form">
<%= form_for(@victim, :url => victims_path(@victim)) do |f| %>
  <% if @victim.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@victim.errors.count, "error") %> prohibited this victim from being saved:</h2>

      <ul>
      <% @victim.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="input-group">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div class="input-group">
    <%= f.label :age %>
    <%= f.number_field :age %>
  </div>
  <div class="input-group">
    <%= f.label :country %>
    <%= f.text_field :country %>
  </div>
  <div class="input-group">
    <%= f.label :city %>
    <%= f.text_field :city %>
  </div>
  <div class="input-group">
    <%= f.label :link %>
    <%= f.text_field :link %>
  </div>
  <div class="actions">
    <%= f.submit 'Add victim'%>
  </div>
<% end %>
</div>
